// Copied by the `copy` action into src/<slugified-name>.js (the destination path
// is templated, not the contents), after which `remove` deletes this templates/
// directory. Demonstrates copy + a templated destination path.
export const component = () => 'hello from a copied component';
